#include "main.h"

/**
 * set_string - sets the value of a pointer to char
 * @s: a pointer to to a pointer that will be set
 * @to: the pointer to the char that will be set
 * Return: None
 */

void set_string(char **s, char *to)
{
*s = to;
}
